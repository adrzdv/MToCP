<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Цвет фона -->
    <stroke
        android:width="1dp"
        android:color="@color/divider_color" /> <!-- Цвет и толщина рамки -->
    <corners android:radius="16dp" /> <!-- Радиус скругления углов -->
</shape>